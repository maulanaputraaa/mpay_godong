import 'package:flutter/material.dart';
import 'laporan_simpanan_menu.dart';

class Body extends StatelessWidget {
  const Body({super.key});

  @override
  Widget build(BuildContext context) {
    return const LaporanMenu();
  }
}
